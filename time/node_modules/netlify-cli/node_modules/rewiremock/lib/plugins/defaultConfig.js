'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _path = require('path');

var _utils = require('./common/utils');

var rewireMockConfig = function rewireMockConfig(rewiremock) {
  var path = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : process.cwd();

  var resolvedConfigPath = (0, _path.resolve)(path, 'rewiremock.config.js');
  var fileName = (0, _utils.fileExists)(resolvedConfigPath);
  if (fileName) {
    var config = (0, _utils.ensureRequire)(fileName);
    (config.default || config)(rewiremock);
  }
};

exports.default = rewireMockConfig;